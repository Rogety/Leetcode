char* reverseString(char* s) {
    int len = strlen(s);
    char* string = (char*)malloc(sizeof(char)*(len+1));
    
    for(int i=0;i<len;i++)
        string[i] = s[len-i-1];
    string[len] = '\0';
        
    return string;
    
}
